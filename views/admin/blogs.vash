@html.extend('layout', function(model) {
  @html.block('content', function(model) {
    @html.block('_toastr_display', function(model){
      @html.include('_toastr_display', model)
    })


<div class="center">
    <h1>@model.title</h1>
    <table class="center">
      <caption>
        All Blogs active and inactive
      </caption>
      <tr>
        <th>Header</th>
        <th>Created</th>
        <th>Updated</th>
        <th>Published?</th>
        <th>Category</th>
        <th>Actions</th>
      </tr>

    @model.blogs.forEach(function(item){
      <tr>
	      <td>@item.header</td>
        <td>@item.createtimestamp</td> 
        <td>@item.modifytimestamp</td>
        <td>@item.ispublished</td>
        <td>@item.category</td>
        <td>
          <ul>
            <li>
              <a href='/admin/blog/update/@item.id'>Edit</a>
            </li>
              @if (item.ispublished){
              <li>
                <a href="/admin/blog/deactivate/@item.id">Deactivate</a>
              </li>
            }else{
              <li>
                <a href="/admin/blog/activate/@item.id">Activate</a>
              </li>
              }
            <li>
              <a href="/admin/blog/delete/@item.id">Delete</a>
            </li>
          </ul>
          </td>
      </tr>
    })
      </table>
      </div>
  })
})