@html.extend('layout', function(model) {
  @html.block('content', function(model) {
    <h1>@model.message</h1>
    <h2>@model.error.status</h2>
    <pre style='color:red;'>@model.error.stack</pre>
  })
})
